name: Nx Cloud Debug

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  actions: read
  contents: read

env:
  NX_VERBOSE_LOGGING: true
  NX_PERF_LOGGING: true
  NX_NATIVE_LOGGING: nx::native::workspace

jobs:
  debuging:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node-version: 20

      # Setup environment as needed to get Nx to run
      # such as any other scripts/commands to run databases/install tooling
      - name: Setup
        run: |
          npm ci

      - name: Nx Report
        run: |
          npx nx report
          npx nx daemon
      - uses: nrwl/nx-set-shas@v4
      - name: 'List Important Env Vars'
        run: |
          echo "GITHUB_REF: $GITHUB_REF";
          echo "GITHUB_BASE_REF: $GITHUB_BASE_REF";
          echo "GITHUB_HEAD_REF: $GITHUB_HEAD_REF";
          echo "GITHUB_REF_NAME: $GITHUB_REF_NAME";
          echo "GITHUB_EVENT_NAME: $GITHUB_EVENT_NAME";
          echo "NX_BRANCH: $NX_BRANCH";
          echo "NX_BASE: $NX_BASE";
          echo "NX_HEAD: $NX_HEAD";

      - name: Run Targets
        run: |
          npx nx reset
          NX_PROFILE=.nx-debug/nx-cloud-ci-no-cache.json npx nx build demo --skip-nx-cache
          npx nx reset
          NX_PROFILE=.nx-debug/nx-cloud-ci-no-cloud.json npx nx build demo --no-cloud
          npx nx reset
          NX_PROFILE=.nx-debug/nx-cloud-ci.json npx nx build demo

      - name: Upload Debug Profiles
        uses: actions/upload-artifact@v4
        with:
          name: ci-run-profiles
          path: .nx-debug/*
      # Once the workflow is ran, there should be a list of artifacts on the workflow,
      # please download along with the agent logs and send to nx cloud support
